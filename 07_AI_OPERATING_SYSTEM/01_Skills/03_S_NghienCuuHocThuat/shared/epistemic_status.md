# Epistemic Status

Mọi đoạn kết luận nhạy cảm nên gắn một trong các mức: `KNOWN`, `SUPPORTED_INFERENCE`, `TENTATIVE`, `SPECULATIVE`, `UNKNOWN`.

Không chuyển `TENTATIVE` thành khẳng định chắc chắn chỉ vì văn phong được chỉnh sửa.
