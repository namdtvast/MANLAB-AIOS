# Change Report — Tier XS/S (bản rút gọn)

Dùng thay cho `verification-report.md` khi việc chỉ ở Tier XS hoặc S — không cần artifact riêng, có thể dán thẳng vào PR description hoặc trả lời cuối hội thoại.

```
Outcome:            <1-3 dòng>
Files changed:      <danh sách>
Tests run / result: <lệnh> → <kết quả> (hoặc NOT RUN — <lý do>)
Diff review:        <chỉ file dự kiến? có gì bất thường không?>
Result:             PASS / PASS WITH WARNINGS / FAIL
```
